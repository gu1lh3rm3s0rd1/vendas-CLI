"""Package marker for the `src` package.

This file makes the `src` directory importable as a package (e.g. `import src.parser`).
It is intentionally minimal.
"""

__all__ = ["parser", "report", "output", "main"]
